import { useState } from "react"

function IndexPopup() {
  const [data, setData] = useState("")

  return (
    <div
      style={{
        display: "flex",
        flexDirection: "column",
        padding: 10,
        width:"85vw",
      }}>
      <div style={{
        
      }}>
        成理小青年
      </div>
    </div>
  )
}

export default IndexPopup
